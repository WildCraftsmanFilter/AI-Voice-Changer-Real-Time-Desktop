#include <stdio.h>
#include <stdlib.h>

/**
 * @brief Performs basic arithmetic operation based on operator and operands.
 * @param a First operand (double)
 * @param b Second operand (double)
 * @param op Operator character (+, -, *, /)
 * @return Result of the operation as double
 * @note Returns 0.0 and prints error message for invalid operations
 */
